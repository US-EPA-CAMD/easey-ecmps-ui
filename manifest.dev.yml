applications:
  - name: ui-portal
    path: .
    command: npm run start
    memory: 1024M
    env:
      OPTIMIZE_MEMORY: true
      REACT_APP_FACILITIES_API: https://easey-dev.app.cloud.gov/api/facilities
    # services:
    #   - camd-pg-db
    # instances: 2
    # health-check-type: http
    # health-check-http-endpoint: /
    routes:
      - route: easey-dev.app.cloud.gov